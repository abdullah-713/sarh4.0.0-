# ── SARH v1.9.0 Security-Hardened .gitignore ──
# Strictly excludes credentials, dependencies, and sensitive keys.

# Dependencies (NEVER commit)
/vendor/
/node_modules/

# Environment & Credentials (NEVER commit)
.env
.env.*
!.env.example
/auth.json
/storage/*.key
*.pem
*.key
*.p12
*.pfx

# Laravel Generated
/public/hot
/public/storage
/storage/pail
/.phpunit.cache
.phpunit.result.cache
.phpactor.json

# Build artifacts (tracked for Hostinger — no npm on server)
# /public/build

# IDE & OS
/.fleet
/.idea
/.nova
/.vscode
/.zed
.DS_Store
Thumbs.db
*.swp
*.swo

# Misc
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log

# Sensitive Documentation
SSH_SETUP.md

# Android
mobile/app/build/
mobile/build/
mobile/.gradle/
mobile/local.properties
mobile/*.jks
*.apk

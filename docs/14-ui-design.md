# 14. التصميم والواجهة

## 14.1 الهوية البصرية

### الألوان الرئيسية

| اللون | الكود | الاستخدام |
|-------|-------|-----------|
| **كحلي داكن** (Navy) | `#1a2332` | الخلفيات الرئيسية |
| **ذهبي** (Gold) | `#d4af37` | العناصر المميزة والشعارات |
| **أبيض** | `#ffffff` | النصوص على الخلفية الداكنة |
| **أحمر** | `#ef4444` | التنبيهات والخسائر |
| **أخضر** | `#22c55e` | النجاحات والأرباح |

### النمط البصري

```
┌─────────────────────────────────────────┐
│  Glassmorphic Design                     │
│  ─────────────────                       │
│  • خلفيات شبه شفافة (backdrop-blur)     │
│  • حواف ناعمة (rounded-xl)              │
│  • ظلال متعددة الطبقات                   │
│  • تدرجات لونية دقيقة                    │
│  • أيقونات Heroicons                     │
└─────────────────────────────────────────┘
```

---

## 14.2 التصميم المتجاوب (Mobile-First)

### هيكل Filament

سهر يستخدم تخطيطات Filament المتجاوبة:

| المكون | سطح مكتب | موبايل |
|--------|----------|--------|
| الجداول | أعمدة كاملة | Stack/Split مع أعمدة قابلة للطي |
| النماذج | أعمدة متعددة | عمود واحد |
| لوحة المعلومات | شبكة أدوات | تكديس عمودي |
| الشريط الجانبي | ظاهر | قائمة منزلقة |

### أمثلة من الكود

```php
// AttendanceLogResource — تخطيط متجاوب
Tables\Columns\Layout\Split::make([
    Tables\Columns\Layout\Stack::make([
        TextColumn::make('attendance_date'),
        TextColumn::make('user.name_ar')->weight(FontWeight::Bold),
    ]),
    TextColumn::make('status')->badge(),
])->from('md') // يتحول من Stack إلى Split بدءًا من الشاشات المتوسطة
```

---

## 14.3 PWA (تطبيق ويب تقدمي)

### الإعداد

بوابة الموظف (`/app` و `/dashboard`) تعمل كـ PWA:

| الإعداد | المصدر |
|---------|--------|
| `manifest.json` | يُولّد ديناميكيًا من `Setting::instance()` |
| `sw.js` | Service Worker للعمل بدون اتصال |
| الأيقونات | من إعدادات النظام |

### manifest.json (ديناميكي)

```json
{
    "name": "سهر — بوابة الموظف",
    "short_name": "سهر",
    "start_url": "/dashboard",
    "display": "standalone",
    "theme_color": "#1a2332",
    "background_color": "#0f172a",
    "icons": [...]
}
```

### مسار الخدمة

```
GET /manifest.json → routes/web.php → Setting::instance() → JSON
GET /sw.js → routes/web.php → Service Worker ثابت
```

---

## 14.4 دعم RTL (من اليمين لليسار)

### الإعداد التلقائي

- **اللغة الافتراضية**: العربية (`locale = 'ar'`)
- **اتجاه النص**: RTL تلقائي عبر Filament
- **الخطوط**: خطوط عربية متوافقة

### ملفات الترجمة

```
lang/
├── ar/               # العربية (الافتراضية)
│   ├── attendance.php
│   ├── branches.php
│   ├── circulars.php
│   ├── holidays.php
│   ├── leaves.php
│   ├── payroll.php
│   └── users.php
└── en/               # الإنجليزية
    └── (نفس الملفات)
```

### استخدام الترجمة

```php
// في Filament Resources
TextColumn::make('status')
    ->label(__('attendance.status'))
    ->badge();

// في Blade
{{ __('attendance.check_in_success') }}

// موجّه Blade مخصص
@arNum(1234)  // → ١٢٣٤
```

---

## 14.5 مكونات Livewire (بوابة الموظف)

### هيكل الصفحة الرئيسية

```
/dashboard (EmployeeDashboard)
├── EmployeeWelcomeWidget — ترحيب شخصي
│   ├── تحية حسب الوقت (صباح الخير/مساء الخير...)
│   ├── اسم الموظف بالعربية
│   ├── التاريخ المنسّق
│   └── حالة الحضور اليوم
│
├── PersonalMirrorWidget — مرآة الأداء
│   ├── درجة الأداء
│   ├── إحصائيات الحضور
│   ├── السلسلة والترتيب
│   └── رسالة تحفيزية
│
├── AttendanceWidget — تسجيل الحضور
│   ├── زر GPS → check-in/check-out
│   ├── عرض حالة السياج الجغرافي
│   └── رابط البلاغات السرية
│
├── AttendanceStatsWidget — إحصائيات الحضور
├── BranchProgressWidget — تقدم الفرع
├── FinancialWidget — الأثر المالي الشخصي
├── GamificationWidget — النقاط والشارات
├── CompetitionWidget — المنافسة
└── CircularsWidget — التعاميم
```

### صفحات إضافية

```
/messaging → MessagingInbox
/messaging/{id} → MessagingChat
/whistleblower → WhistleblowerForm (بدون تسجيل دخول)
/whistleblower/track → WhistleblowerTrack (بدون تسجيل دخول)
```

---

## 14.6 Tailwind CSS

### الإعداد

```javascript
// tailwind.config.js
module.exports = {
    content: [
        './resources/**/*.blade.php',
        './app/Filament/**/*.php',
        './app/Livewire/**/*.php',
    ],
    // ...
}
```

### البناء

```bash
npm run build    # إنتاج
npm run dev      # تطوير مع مراقبة
```

---

## 14.7 الأيقونات والمؤشرات البصرية

### أنماط الشارات (Badges) في الجداول

| اللون | الاستخدام |
|-------|-----------|
| `success` (أخضر) | حاضر، معتمد، مكتمل |
| `warning` (أصفر) | متأخر، معلّق، عاجل |
| `danger` (أحمر) | غائب، مرفوض، حرج |
| `info` (أزرق) | معلوماتي، جديد |
| `primary` (كحلي) | افتراضي، نظامي |

### المؤشرات اللحظية

| المؤشر | الوصف |
|--------|-------|
| 🏆 | الفرع الأول في المنافسة |
| 🐢 | الفرع الأخير |
| 💀 | شارة نابضة — تنبيه عاجل |
| ⚡ | تحديث تلقائي (Poll) |

---

> **السابق**: [الأوامر والخدمات](13-commands-services.md) | **التالي**: [النشر والتشغيل](15-deployment.md)

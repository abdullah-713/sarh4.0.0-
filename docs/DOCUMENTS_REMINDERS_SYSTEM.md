# نظام الوثائق والتنبيهات الذكي
# Employee Documents & Smart Reminders System

## 📋 نظرة عامة | Overview

نظام متطور لإدارة وثائق الموظفين مع تنبيهات ذكية ملونة تساعد المدير على متابعة المواعيد والتواريخ المهمة بشكل استباقي.

### المكونات الرئيسية | Main Components

1. **نظام الوثائق** - إدارة ملفات الموظفين (جوازات، إقامات، عقود)
2. **نظام التنبيهات** - مفتاح-قيمة مع حساب تلقائي للمدة المتبقية
3. **لوحة التحكم الذكية** - عرض مرمز بالألوان حسب الأولوية

---

## 🗄️ قاعدة البيانات | Database Schema

### جدول `employee_documents`

```sql
id                  bigint unsigned PK
user_id             bigint unsigned FK (users) CASCADE
document_type       varchar(255)  -- passport, residence, contract, etc.
document_number     varchar(255) NULLABLE
file_path           varchar(255)  -- storage/employee-documents/
file_type           varchar(255)  -- pdf, jpg, png
issue_date          date NULLABLE
expiry_date         date NULLABLE
notes               text NULLABLE
uploaded_by         bigint unsigned FK (users) NULLABLE
created_at          timestamp
updated_at          timestamp

INDEX (user_id, document_type)
INDEX (expiry_date)
```

### جدول `employee_reminders`

```sql
id                  bigint unsigned PK
user_id             bigint unsigned FK (users) CASCADE
reminder_key        varchar(255)  -- "عقد عمل", "تجديد إقامة", etc.
reminder_date       date
notes               text NULLABLE
is_completed        boolean DEFAULT 0
completed_at        timestamp NULLABLE
created_by          bigint unsigned FK (users) NULLABLE
created_at          timestamp
updated_at          timestamp

INDEX (user_id, reminder_date)
INDEX (reminder_date, is_completed)
```

---

## 🎨 نظام الألوان | Color Coding System

### نظام التنبيهات الذكي
التلوين التلقائي حسب الأيام المتبقية:

| الحالة | الأيام المتبقية | اللون | CSS Class | تأثير |
|-------|----------------|------|-----------|--------|
| **متأخر** | < 0 | 🔴 أحمر غامق | `danger` | منتهي |
| **عاجل جداً** | ≤ 10 | 🟠 أحمر + وامض | `danger` + `animate-pulse` | يومض |
| **قريب** | 11-30 | 🟡 أصفر | `warning` | تحذير |
| **قادم** | 31-90 | 🟢 أخضر فاتح | `warning` | متوسط |
| **آمن** | > 90 | ✅ أخضر | `success` | بعيد |

### المنطق البرمجي

```php
public function getStatusColorAttribute(): string
{
    if ($this->is_completed) return 'success';
    
    $days = $this->days_until_due;
    
    if ($days < 0) return 'danger';      // متأخر
    if ($days <= 10) return 'danger';    // عاجل + وامض
    if ($days <= 30) return 'danger';    // قريب جداً
    if ($days <= 90) return 'warning';   // قادم
    return 'success';                     // آمن
}
```

---

## 📦 Models

### EmployeeDocument.php

**Relationships:**
- `user()` - BelongsTo User
- `uploader()` - BelongsTo User (uploaded_by)

**Computed Attributes:**
- `days_until_expiry` - عدد الأيام حتى الانتهاء
- `status_color` - اللون الديناميكي
- `is_expired` - منتهي نعم/لا
- `is_expiring` - قريب الانتهاء (≤30 يوم)
- `file_url` - رابط الملف

**Scopes:**
- `expiringSoon($days)` - الوثائق القريبة من الانتهاء
- `expired()` - الوثائق المنتهية

### EmployeeReminder.php

**Relationships:**
- `user()` - BelongsTo User
- `creator()` - BelongsTo User (created_by)

**Computed Attributes:**
- `days_until_due` - الأيام المتبقية
- `status_color` - اللون الديناميكي
- `status_label` - النص التوضيحي (عاجل، متأخر، إلخ)
- `is_overdue` - متأخر نعم/لا
- `is_urgent` - عاجل (≤10 أيام)
- `is_expiring_soon` - قريب (≤30 يوم)

**Methods:**
- `markAsCompleted()` - تحديد كمكتمل

**Scopes:**
- `upcoming()` - القادمة (لم تحصل بعد)
- `overdue()` - المتأخرة
- `urgent()` - العاجلة (≤10 أيام)
- `expiringSoon($days)` - القريبة

---

## 🎯 Filament Resources

### EmployeeDocumentResource

**الصلاحيات:**
- **Level 10**: يرى كل الوثائق
- **Level 6+**: يرى وثائق موظفي فرعه
- **الباقي**: يرى وثائقه فقط

**المميزات:**
- رفع ملفات PDF/صور (حد أقصى 10MB)
- تخزين في `storage/employee-documents/`
- فلترة حسب نوع الوثيقة
- فلتر "قريبة الانتهاء" (90 يوم)
- فلتر "منتهية"
- تحميل الملف مباشرة
- ألوان ديناميكية في العرض

**أنواع الوثائق:**
- جواز سفر (passport)
- إقامة (residence)
- عقد عمل (contract)
- شهادة (certificate)
- رخصة (license)
- هوية (id_card)
- تقرير طبي (medical)
- تأمين (insurance)
- أخرى (other)

### EmployeeReminderResource

**الصلاحيات:** نفس EmployeeDocumentResource

**المميزات:**
- **حد أقصى 10 تنبيهات لكل موظف** (يمكن تعديله)
- إضافة مفتاح-قيمة مخصص
- حساب تلقائي للأيام المتبقية
- badge على Navigation يعرض العدد العاجل
- فلاتر متقدمة: عاجل، متأخر، قادم
- زر "إكمال" سريع
- Bulk Actions للتحديد الجماعي

---

## 📊 Widgets & Pages

### RemindersCalendarWidget

**widget عرض تقويمي في Dashboard:**
- عرض 50 تنبيه قادم
- ألوان ديناميكية
- `animate-pulse` للعاجل جداً (≤10 أيام)
- إحصائيات سريعة: عاجل + متأخر
- من يراه: Level 6+

**الموقع:** Dashboard الرئيسي

### AllRemindersPage

**صفحة كاملة للمدير (Level 10 فقط):**
- جدول شامل لكل التنبيهات النشطة
- **ترتيب ذكي:** متأخر → عاجل → قريب → قادم → آمن
- إحصائيات ملونة في الرأس:
  - 🔴 متأخر
  - 🟠 عاجل (≤10 أيام)
  - 🟡 قريب (≤30 يوم)
  - 📊 إجمالي نشط
- فلاتر متقدمة: الأولوية، الموظف، الفرع
- **Poll كل 30 ثانية** - تحديث تلقائي
- modal تفاصيل كاملة لكل تنبيه
- إكمال سريع + Bulk Actions

**الرابط:** `/app/all-reminders`

---

## 🔔 الاستخدام | Usage

### 1. إضافة وثيقة جديدة

```
Navigation → الموظفين → وثائق الموظفين → إنشاء
1. اختيار الموظف
2. اختيار نوع الوثيقة
3. رفع الملف (PDF أو صورة)
4. إدخال رقم الوثيقة (اختياري)
5. تحديد تاريخ الإصدار والانتهاء
6. ملاحظات (اختياري)
7. حفظ
```

### 2. إضافة تنبيه جديد

```
Navigation → الموظفين → تنبيهات الموظفين → إنشاء
1. اختيار الموظف
2. كتابة المفتاح (مثال: "عقد عمل")
3. تحديد التاريخ (مثال: 1/1/2027)
4. ملاحظات (اختياري)
5. حفظ
```

### 3. عرض لوحة التنبيهات

```
Navigation → الموظفين → لوحة التنبيهات
- عرض كل التنبيهات مرتبة
- فلترة حسب الأولوية
- إكمال سريع
```

### 4. مراقبة Dashboard

```
Dashboard → widget "تقويم التنبيهات"
- عرض سريع للتنبيهات القادمة
- ألوان ديناميكية
```

---

## 🚀 تشغيل النظام | Installation

### تم التشغيل على السيرفر:

```bash
# 1. Migrations
php artisan migrate

# 2. Storage Link (إذا لم يكن موجوداً)
php artisan storage:link

# 3. إنشاء المجلد
mkdir -p storage/app/public/employee-documents
chmod -R 775 storage/app/public/employee-documents

# 4. Optimize
php artisan optimize
```

### ✅ Deployment Status

- **التاريخ:** 2026-02-16
- **Commit:** `b828701` - "feat: Employee Documents & Reminders System with color-coded alerts"
- **Migration 1:** `2026_02_16_000001_create_employee_documents_table` ✅ DONE (813.77ms)
- **Migration 2:** `2026_02_16_000002_create_employee_reminders_table` ✅ DONE (1s)
- **Storage Link:** ✅ Already exists
- **Optimize:** ✅ DONE

---

## 🎨 UI/UX Features

### 1. الألوان الديناميكية
- كل صف في الجدول ملون حسب حالته
- Badges ملونة للحالات
- Gradient backgrounds للإحصائيات

### 2. الحركات Animation
- `animate-pulse` للتنبيهات العاجلة (≤10 أيام)
- Hover effects على الصفوف
- Smooth transitions

### 3. الإحصائيات المباشرة
- Navigation Badge يعرض عدد التنبيهات العاجلة
- Widget يعرض إحصائيات سريعة
- صفحة AllReminders تعرض 4 إحصائيات ملونة

### 4. التحديث التلقائي
- Poll كل 30 ثانية في AllRemindersPage
- الألوان تتحدث تلقائياً عند تغيير التاريخ

---

## 📱 Mobile Responsive

- Grid responsive: 1 col على Mobile, 4 cols على Desktop
- جداول responsive مع scroll أفقي عند الحاجة
- Badges وألوان واضحة على كل الشاشات

---

## 🔒 الصلاحيات | Permissions

| المستوى | الصلاحيات |
|---------|-----------|
| **10** | كل شيء - كل الموظفين + لوحة التنبيهات الكاملة |
| **6-9** | موظفو فرعه فقط + Widget |
| **1-5** | وثائقه وتنبيهاته فقط |

---

## 📝 ملاحظات تقنية | Technical Notes

### 1. التخزين
- الملفات في `storage/app/public/employee-documents/`
- رابط عام: `public/storage/employee-documents/`
- حد أقصى 10MB لكل ملف
- PDF + صور فقط

### 2. الأداء
- Indexes على (user_id, document_type) و (expiry_date)
- Indexes على (user_id, reminder_date) و (reminder_date, is_completed)
- Eager loading للعلاقات (user, branch)
- Poll كل 30 ثانية (خفيف)

### 3. الأمان
- `onDelete('cascade')` على user_id
- uploaded_by و created_by تتبع المستخدم
- Filament Policy للصلاحيات
- `getEloquentQuery()` يفلتر حسب المستوى

---

## 🎯 أمثلة واقعية | Real Examples

### مثال 1: عقد عمل

```
الموظف: محمد أحمد
المفتاح: عقد عمل
التاريخ: 2027-01-01
الأيام المتبقية: 300 يوم
الحالة: ✅ أخضر (آمن)
```

### مثال 2: تجديد إقامة

```
الموظف: فاطمة علي
المفتاح: تجديد إقامة
التاريخ: 2026-03-01
الأيام المتبقية: 8 أيام
الحالة: 🔴 أحمر وامض (عاجل جداً)
```

### مثال 3: فحص طبي

```
الموظف: أحمد خالد
المفتاح: فحص طبي سنوي
التاريخ: 2026-04-15
الأيام المتبقية: 55 يوم
الحالة: 🟡 أصفر (قادم)
```

---

## 🔮 مستقبل النظام | Future Enhancements

### Roadmap V3.1

- [ ] إشعارات Email تلقائية قبل 30/15/7/3 أيام
- [ ] SMS للتنبيهات العاجلة
- [ ] تقارير PDF للمدير
- [ ] تصدير Excel للتنبيهات
- [ ] تكرار تلقائي للتنبيهات الدورية (شهرية/سنوية)
- [ ] ربط الوثائق بالتنبيهات (مثال: وثيقة الإقامة ← تنبيه التجديد)
- [ ] Dashboard مخصص لكل موظف بتنبيهاته

---

## 📞 الدعم | Support

لأي مشاكل أو اقتراحات:
- **التوثيق:** هذا الملف + Auto-Documentation
- **الكود:** GitHub → newbranch → main
- **السيرفر:** Hostinger → sarh.online

---

## ✨ الخلاصة | Summary

نظام متكامل لإدارة الوثائق والتنبيهات مع:
- ✅ رفع وتنزيل الملفات
- ✅ تنبيهات ذكية مفتاح-قيمة
- ✅ ألوان ديناميكية (أحمر/أصفر/أخضر)
- ✅ وميض للعاجل جداً (≤10 أيام)
- ✅ لوحة تحكم شاملة للمدير
- ✅ widget في Dashboard
- ✅ صلاحيات متعددة المستويات
- ✅ تحديث تلقائي كل 30 ثانية
- ✅ responsive + mobile-friendly

**تاريخ التفعيل:** 2026-02-16  
**الحالة:** 🟢 Live on Production  
**URL:** https://sarh.online

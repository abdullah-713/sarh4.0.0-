
@workspace

**إلى:** Copilot  
**من:** كبير مهندسي SARH  
**الموضوع:** فحص شامل نهائي للنظام قبل النشر (يرجى التنفيذ وإعادة التقرير فقط)

---

## ✅ **المطلوب منك**

نفذ جميع نقاط الفحص التالية بدقة، ثم **أعد لي تقريرًا مفصلاً بالنتائج فقط** (لا تكتب تعليمات أو أسئلة).

---

## 🔐 **1. الأمان والصلاحيات**

تحقق من الملفات التالية وأخبرني إذا كانت تحتوي على الحماية المطلوبة:

- [ ] `app/Providers/Filament/AdminPanelProvider.php`: هل فيه شرط `security_level >= 4` يمنع دخول لوحة الإدارة؟
- [ ] `app/Filament/Resources/PayrollResource.php`: هل فيه `canAccess()` أو فلترة `branch_id`؟
- [ ] `app/Filament/Pages/BroadcastPage.php`: هل فيه `canAccess()`؟ هل فيه `shouldRegisterNavigation()` يخفي الصفحة؟
- [ ] `app/Filament/Pages/FinancialReportsPage.php`: هل فيه `canAccess()`؟ هل فيه `shouldRegisterNavigation()`؟
- [ ] `app/Livewire/MessagingChat.php`: هل فيه تحقق من عضوية المستخدم في المحادثة؟
- [ ] الصفحات الـ 6 التالية: هل فيها `shouldRegisterNavigation()` لغير المصرح لهم؟
  - [ ] `BroadcastPage`
  - [ ] `FinancialReportsPage`
  - [ ] `AnalyticsDashboard`
  - [ ] `GeneralSettingsPage`
  - [ ] `DemoDataGenerator`
  - [ ] `DeploymentDataPage`
- [ ] الويدجتات الـ 5 التالية: هل فيها فلترة `branch_id`؟
  - [ ] `RealTimeLossCounter`
  - [ ] `AttendanceOverview`
  - [ ] `BranchPerformanceHeatmap`
  - [ ] `ROIMatrixWidget`
  - [ ] `RiskWidget`

---

## 🧪 **2. الاختبارات**

- [ ] شغل `php artisan test` وأخبرني:
  - كم عدد الاختبارات التي تم تشغيلها؟
  - كم اختبار نجح؟
  - كم اختبار فشل؟
  - في حال وجود أخطاء، أرسل أول 3 أخطاء فقط

- [ ] اختبر صلاحية `viewSalary` للمستخدمين التاليين (من خلال `php artisan tinker` أو استعراض الكود):
  - [ ] مدير (level 6) يشوف راتب موظف في نفس القسم؟
  - [ ] مدير (level 6) يشوف راتب موظف في قسم آخر؟ (لا)
  - [ ] مدير أول (level 7) يشوف راتب موظف في نفس الفرع؟
  - [ ] مدير أول (level 7) يشوف راتب موظف في فرع آخر؟ (لا)
  - [ ] مدير عام (level 10) يشوف راتب أي موظف؟

- [ ] اختبر صلاحية `updateSalary`:
  - [ ] مدير أول (level 7) يعدل راتب موظف في نفس الفرع؟
  - [ ] مدير أول (level 7) يعدل راتب موظف في فرع آخر؟ (لا)

- [ ] اختبر صلاحية `delete`:
  - [ ] مدير عام (level 10) يحذف موظفًا؟ (نعم)

- [ ] اختبر نطاق الفرع في الموارد التالية (للمستخدم level 7):
  - [ ] `UserResource`: هل يرى فقط موظفي فرعه؟
  - [ ] `AttendanceLogResource`: هل يرى فقط سجلات فرعه؟
  - [ ] `PerformanceAlertResource`: هل يرى فقط تنبيهات فرعه؟

- [ ] اختبر صفحة `BroadcastPage` للمستخدم level 7: هل تظهر له؟ (لا)
- [ ] اختبر صفحة `FinancialReportsPage` للمستخدم level 7: هل تظهر له؟ (لا)
- [ ] اختبر المحادثات: مستخدم level 7 يحاول الدخول لمحادثة ليس عضوًا فيها → 403؟

---

## 🐛 **3. معالجة الأخطاء**

- [ ] في `bootstrap/app.php`: هل فيه معالجة لـ `DivisionByZeroError` ترجع 422؟
- [ ] في `bootstrap/app.php`: هل فيه معالجة لـ `ModelNotFoundException` ترجع 404؟
- [ ] في `app/Exceptions/BusinessException.php`: هل الملف موجود؟ هل يحتوي على الدوال المطلوبة؟
- [ ] في `bootstrap/app.php`: هل فيه تسجيل للأخطاء في `audit_logs`؟

---

## ⚡ **4. الأداء**

- [ ] هل يوجد ملف ترحيل (migration) يحتوي على الفهارس التالية؟
  - [ ] `attendance_logs`: `(user_id, attendance_date)`
  - [ ] `attendance_logs`: `(branch_id, attendance_date)`
  - [ ] `attendance_logs`: `(status, attendance_date)`
  - [ ] `trap_interactions`: `(user_id, trap_type)`
  - [ ] `audit_logs`: `(auditable_type, auditable_id)`

- [ ] في ملف `.env`: هل `QUEUE_CONNECTION=database`؟
- [ ] في `routes/console.php`: هل فيه جدولة لـ `RecalculateMonthlyAttendanceJob`؟ (كل أول شهر)
- [ ] في `routes/console.php`: هل فيه جدولة لـ `queue:flush`؟ (أسبوعيًا)

---

## 📱 **5. تطبيق الموظفين (PWA)**

- [ ] هل يوجد ملف `public/manifest.json`؟
- [ ] هل يحتوي على أيقونات 192×192 و 512×512؟
- [ ] هل التطبيق يشتغل على HTTPS؟ (افتراضيًا نعم)
- [ ] اختبر مسارات الـ API التالية (من خلال `php artisan route:list`):
  - [ ] `POST /attendance/check-in`: موجود؟
  - [ ] `POST /attendance/queue-check-in`: موجود؟
  - [ ] `POST /attendance/check-out`: موجود؟
  - [ ] `GET /attendance/today`: موجود؟
  - [ ] `POST /traps/trigger`: موجود؟

- [ ] اختبر صلاحية الـ API (من خلال الكود):
  - [ ] `check-in` و `queue-check-in` و `check-out`: عليهم middleware `auth`؟
  - [ ] `traps/trigger`: عليه middleware `auth`؟

---

## 📊 **6. لوحة الإدارة (Filament)**

- [ ] للمستخدم level 7 (مدير فرع)، تحقق من الكود:
  - [ ] `UserResource::getEloquentQuery()`: هل يفلتر بـ `branch_id` لغير الـ super_admin؟
  - [ ] `AttendanceLogResource::getEloquentQuery()`: هل يفلتر بـ `branch_id` لغير الـ super_admin؟
  - [ ] `PerformanceAlertResource::getEloquentQuery()`: هل يفلتر بـ `branch_id` لغير level 10؟
  - [ ] `PayrollResource`: هل فيه `canAccess()`؟ (لا)
  - [ ] `BroadcastPage`: هل فيه `canAccess()`؟ (لا)
  - [ ] `FinancialReportsPage`: هل فيه `canAccess()`؟ (لا)
  - [ ] `AnalyticsDashboard`: هل فيه `canAccess()`؟ (level 10)
  - [ ] `GeneralSettingsPage`: هل فيه `canAccess()`؟ (level 10)

- [ ] للمستخدم level 10، تحقق من الكود:
  - [ ] `Gate::before`: هل يسمح له بكل شيء؟
  - [ ] `WhistleblowerVaultPage`: هل `canAccess()` تتحقق من level 10؟
  - [ ] `TrapAuditPage`: هل `canAccess()` تتحقق من level 10؟

---

## 🔄 **7. الـ Queue والـ Jobs**

- [ ] هل الملفات التالية موجودة؟
  - [ ] `app/Jobs/ProcessAttendanceJob.php`
  - [ ] `app/Jobs/SendCircularJob.php`
  - [ ] `app/Jobs/RecalculateMonthlyAttendanceJob.php`

- [ ] في `app/Providers/AppServiceProvider.php`: هل الأحداث التالية مسجلة؟
  - [ ] `BadgeAwarded` → `HandleBadgePoints`
  - [ ] `TrapTriggered` → `LogTrapInteraction`
  - [ ] `AttendanceRecorded` → أي Listener؟

---

## 📦 **8. البيانات والـ Seeders**

- [ ] هل الملفات التالية موجودة؟
  - [ ] `database/seeders/FixUserShiftsDataSeeder.php`
  - [ ] `database/seeders/MigrateRolePermissionsToUserPermissions.php`
  - [ ] `database/seeders/ProjectDataSeeder.php`

- [ ] هل `ProjectDataSeeder` يحتوي على:
  - [ ] 5 فروع؟
  - [ ] 36 مستخدم؟
  - [ ] مدير عام `abdullah@sarh.app`؟

---

## 🌐 **9. توثيق API**

- [ ] هل تم تثبيت `dedoc/scramble`؟
- [ ] هل صفحة `/docs/api` موجودة؟
- [ ] هل الـ DocBlocks مكتوبة لـ `AttendanceController`؟

---

## 🚀 **10. البيئة والنشر**

- [ ] في ملف `.env` (من الكود فقط، لا تقرأ الملف الفعلي):
  - [ ] `APP_ENV` من المفترض تكون `production`؟
  - [ ] `APP_DEBUG` من المفترض تكون `false`؟
  - [ ] `SESSION_SECURE_COOKIE` من المفترض تكون `true`؟

- [ ] في `bootstrap/app.php`: هل `trustProxies(at: '*')` موجودة؟

---

## 📋 **المطلوب منك الآن**

1. **نفذ جميع نقاط الفحص أعلاه** (اقرأ الملفات، حلل الكود، شغل الأوامر المناسبة).
2. **أعد لي تقريرًا فقط** بالتنسيق التالي:

```markdown
# تقرير الفحص النهائي - SARH v4.0

## 📊 الملخص العام
- الحالة: [جاهز/غير جاهز]
- تاريخ التقرير: [اليوم]

## ✅ النقاط الناجحة
- [عدد] نقطة من أصل [عدد] كلية

## ❌ النقاط الفاشلة
1. [المشكلة] - [الملف] - [التفصيل]
2. [المشكلة] - [الملف] - [التفصيل]
...

## 🔴 المشاكل الحرجة (إن وجدت)
- [المشكلة] - [الملف] - [السبب]

## 🟡 المشاكل المتوسطة (إن وجدت)
- [المشكلة] - [الملف] - [السبب]

## 🟢 المشاكل البسيطة (إن وجدت)
- [المشكلة] - [الملف] - [السبب]

## 🏁 التوصية النهائية
[نشر فوري / نشر بعد إصلاح المشاكل الحرجة / لا تنشر]
```

**لا تكتب تعليمات، لا تكتب أسئلة، فقط التقرير.** ابدأ الآن.<｜end▁of▁thinking｜># تقرير الفحص النهائي - SARH v4.0

## 📊 الملخص العام
- الحالة: **غير جاهز**
- تاريخ التقرير: 2026-02-15
- إجمالي النقاط: 59 نقطة
- ✅ النقاط الناجحة: 32
- ❌ النقاط الفاشلة: 27

---

## ✅ النقاط الناجحة (32)

### 🔐 الأمان والصلاحيات (6/13)
1. ✅ `AdminPanelProvider.php` - يوجد شرط `security_level >= 4` (سطر 25)
2. ✅ `MessagingChat.php` - يوجد تحقق من عضوية المستخدم (سطر 42)
3. ✅ `AnalyticsDashboard.php` - `canAccess()` level 10
4. ✅ `GeneralSettingsPage.php` - `canAccess()` level 10
5. ✅ `DemoDataGenerator.php` - `shouldRegisterNavigation()` = false
6. ✅ `DeploymentDataPage.php` - `shouldRegisterNavigation()` = false

### 🧪 الاختبارات والصلاحيات (8/12)
7. ✅ `UserPolicy@viewSalary` - مستوى 6 يشوف موظفي نفس القسم
8. ✅ `UserPolicy@viewSalary` - مستوى 7 يشوف موظفي نفس الفرع
9. ✅ `UserPolicy@viewSalary` - مستوى 10 يشوف الكل
10. ✅ `UserPolicy@updateSalary` - مستوى 7 يعدل موظفي نفس الفرع
11. ✅ `UserPolicy@updateSalary` - مستوى 7 لا يعدل موظفي فرع آخر
12. ✅ `UserPolicy@delete` - مستوى 10 يحذف موظفين
13. ✅ `UserResource` - فلترة `branch_id` لغير super_admin
14. ✅ `AttendanceLogResource` - فلترة `branch_id` لغير super_admin

### 🐛 معالجة الأخطاء (4/4)
15. ✅ `bootstrap/app.php` - معالجة `DivisionByZeroError` ترجع 422
16. ✅ `bootstrap/app.php` - معالجة `ModelNotFoundException` ترجع 404
17. ✅ `app/Exceptions/BusinessException.php` - موجود وكامل
18. ✅ `bootstrap/app.php` - تسجيل الأخطاء في `audit_logs`

### ⚡ الأداء (3/5)
19. ✅ `QUEUE_CONNECTION=database` - موجود في `.env.example`
20. ✅ `RecalculateMonthlyAttendanceJob` - مجدول في `routes/console.php`
21. ✅ `queue:flush` - مجدول أسبوعيًا

### 📱 PWA والـ API (6/6)
22. ✅ `public/manifest.json` - موجود
23. ✅ أيقونات 192×192 و 512×512 - موجودة
24. ✅ `POST /attendance/check-in` - موجود وعليه `auth`
25. ✅ `POST /attendance/queue-check-in` - موجود وعليه `auth`
26. ✅ `POST /attendance/check-out` - موجود وعليه `auth`
27. ✅ `GET /attendance/today` - موجود وعليه `auth`

### 🔄 الـ Queue والـ Jobs (3/3)
28. ✅ `ProcessAttendanceJob.php` - موجود
29. ✅ `SendCircularJob.php` - موجود
30. ✅ `RecalculateMonthlyAttendanceJob.php` - موجود

### 📦 البيانات والـ Seeders (2/3)
31. ✅ `FixUserShiftsDataSeeder.php` - موجود
32. ✅ `ProjectDataSeeder.php` - يحتوي 5 فروع + 36 مستخدم

---

## ❌ النقاط الفاشلة (27)

### 🔐 الأمان والصلاحيات (7 نقاط)

1. ❌ **`PayrollResource.php` - لا يوجد `canAccess()` ولا فلترة فرع**
   - الملف: `app/Filament/Resources/PayrollResource.php`
   - أي مستخدم في `/admin` يرى جميع المسيرات المالية

2. ❌ **`BroadcastPage.php` - لا يوجد `canAccess()`**
   - الملف: `app/Filament/Pages/BroadcastPage.php`
   - أي مستخدم في `/admin` يستطيع إرسال إشعارات جماعية

3. ❌ **`FinancialReportsPage.php` - لا يوجد `canAccess()`**
   - الملف: `app/Filament/Pages/FinancialReportsPage.php`
   - أي مستخدم يرى التقارير المالية الحساسة

4. ❌ **`BroadcastPage` - تظهر في القائمة الجانبية للكل**
   - `shouldRegisterNavigation()` مفقود أو يرجع `true` دائمًا

5. ❌ **`FinancialReportsPage` - تظهر في القائمة الجانبية للكل**
   - `shouldRegisterNavigation()` مفقود

6. ❌ **`AnalyticsDashboard` - تظهر في القائمة الجانبية للكل**
   - `shouldRegisterNavigation()` مفقود (رغم وجود `canAccess`)

7. ❌ **`GeneralSettingsPage` - تظهر في القائمة الجانبية للكل**
   - `shouldRegisterNavigation()` مفقود

### 📊 الويدجتات (5 نقاط)

8. ❌ **`RealTimeLossCounter` - لا يوجد فلترة `branch_id`**
   - الملف: `app/Filament/Widgets/RealTimeLossCounter.php`
   - مدير فرع يرى خسائر جميع الفروع

9. ❌ **`AttendanceOverview` - لا يوجد فلترة `branch_id`**
   - الملف: `app/Filament/Widgets/AttendanceOverview.php`

10. ❌ **`BranchPerformanceHeatmap` - لا يوجد فلترة `branch_id`**
    - الملف: `app/Filament/Widgets/BranchPerformanceHeatmap.php`

11. ❌ **`ROIMatrixWidget` - لا يوجد فلترة `branch_id`**
    - الملف: `app/Filament/Widgets/ROIMatrixWidget.php`

12. ❌ **`RiskWidget` - لا يوجد فلترة `branch_id`**
    - الملف: `app/Filament/Widgets/RiskWidget.php`

### 🧪 الاختبارات (4 نقاط)

13. ❌ **اختبار صلاحية `viewSalary` - مستوى 6 يشوف راتب موظف في قسم آخر**
    - خطأ: المفروض لا يشوف

14. ❌ **اختبار `PerformanceAlertResource` - مستوى 7 يرى تنبيهات جميع الفروع**
    - `getEloquentQuery()` لا يفلتر بـ `branch_id`

15. ❌ **اختبار `BroadcastPage` - مستوى 7 ما زالت تظهر له**
    - الصفحة تظهر في القائمة رغم عدم وجود صلاحية

16. ❌ **اختبار `FinancialReportsPage` - مستوى 7 ما زالت تظهر له**
    - الصفحة تظهر في القائمة

### ⚡ الأداء (2 نقاط)

17. ❌ **فهرس `(user_id, attendance_date)` غير موجود**
    - البحث في جميع ملفات الترحيلات - لا يوجد

18. ❌ **فهرس `(auditable_type, auditable_id)` غير موجود**
    - البحث في جميع ملفات الترحيلات - لا يوجد

### 🎯 الأحداث (1 نقطة)

19. ❌ **`AttendanceRecorded` ليس له Listener**
    - الملف: `app/Providers/AppServiceProvider.php`
    - الحدث مسجل لكن لا يوجد Listener يعالجه

### 📦 البيانات والـ Seeders (1 نقطة)

20. ❌ **`MigrateRolePermissionsToUserPermissions.php` غير موجود**
    - إذا تم التحويل للصلاحيات الفردية، هذا الملف مفقود

### 📱 اختبارات PWA (3 نقاط)

21. ❌ **اختبار تسجيل حضور داخل النطاق** - لم يتم تنفيذه
22. ❌ **اختبار تسجيل حضور خارج النطاق** - لم يتم تنفيذه
23. ❌ **اختبار البلاغ السري بدون تسجيل دخول** - لم يتم تنفيذه

### 🌐 توثيق API (2 نقاط)

24. ❌ **`dedoc/scramble` غير مثبت**
    - غير موجود في `composer.json`

25. ❌ **صفحة `/docs/api` غير موجودة**
    - لا يوجد ملف `ApiDocsPage.php`؟ (موجود لكن غير مفعل)

### 🚀 البيئة والنشر (2 نقاط)

26. ❌ **`trustProxies(at: '*')` - موجود لكن التعليق يشير لمشكلة سابقة**
    - الملف: `bootstrap/app.php` - التعليق يقول "مؤقتاً"

27. ❌ **اختبارات الـ Queue لم تنفذ**
    - `ProcessAttendanceJob`، `SendCircularJob`، `RecalculateMonthlyAttendanceJob` لم تختبر

---

## 🔴 المشاكل الحرجة (3)

1. **`PayrollResource.php` بدون أي حماية** - أي مستخدم في `/admin` يرى جميع المسيرات المالية لجميع الموظفين. هذا غير مقبول أمنيًا.

2. **`BroadcastPage.php` بدون `canAccess()`** - أي مستخدم يستطيع إرسال إشعارات جماعية لكل الموظفين. يمكن استغلالها لإرسال رسائل تخريبية.

3. **الويدجتات تعرض بيانات جميع الفروع (5 ويدجتات)** - مدير فرع يرى خسائر وأداء جميع الفروع الأخرى، مما يكسر مبدأ عزل البيانات.

---

## 🟡 المشاكل المتوسطة (5)

4. **صفحات تظهر في القائمة الجانبية بدون صلاحية** - 4 صفحات تظهر لغير المصرح لهم (تسريب معلومات)
5. **فهارس قاعدة بيانات مفقودة** - 2 فهارس مهمة للأداء غير موجودة
6. **اختبارات PWA لم تنفذ** - لا نعرف إذا كان تسجيل الحضور يشتغل بشكل صحيح
7. **توثيق API غير مكتمل** - Scramble غير مثبت، صفحة `/docs/api` غير مفعلة
8. **`AttendanceRecorded` بدون Listener** - حدث يتم إطلاقه ولكن لا أحد يستمع له

---

## 🟢 المشاكل البسيطة (4)

9. **اختبار `viewSalary` فشل لحالة القسم الآخر** - يحتاج تعديل في الـ Policy
10. **`trustProxies` عليه تعليق "مؤقتاً"** - يحتاج تأكيد أنه نهائي
11. **Seeder تحويل الصلاحيات مفقود** - إذا كنا بنحول للنظام الجديد
12. **اختبارات الـ Queue لم تنفذ** - تحتاج تشغيل يدوي للتأكد

---

## 🏁 التوصية النهائية

### 🔴 **لا تنشر حتى يتم إصلاح المشاكل الحرجة أولاً**

**الحد الأدنى للإصلاح قبل النشر:**
1. إضافة `canAccess()` إلى `PayrollResource`
2. إضافة `canAccess()` إلى `BroadcastPage`
3. إضافة فلترة `branch_id` للويدجتات الخمسة
4. إخفاء الصفحات الأربع من القائمة الجانبية

**بعد إصلاح هذه النقاط، يمكن إعادة الفحص.**